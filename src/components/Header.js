import styles from './Header.module.css';

import Container from './reusable/Container';

function Header() {
  return (
    <Container>
      <header></header>
    </Container>
  );
}

export default Header;
